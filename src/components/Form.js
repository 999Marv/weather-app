import { Fragment, useState } from "react";
import classes from "./Form.module.css";

const Form = (props) => {
  return (
    <Fragment>
      <input type="text" />
    </Fragment>
  );
};

export default Form;
